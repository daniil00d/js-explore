// Эталонные ответы к лабе «Предскажи результат» с разбором каждого случая.

export const answers = {
  // Объявление var поднимается наверх функции, присваивание остаётся на месте.
  // К моменту чтения переменная существует и равна undefined.
  varBeforeDeclaration: undefined,

  // let тоже поднимается, но до строки объявления живёт в temporal dead zone.
  // Обращение к ней — не «переменной нет», а «к ней пока нельзя».
  letBeforeDeclaration: 'ReferenceError',

  // typeof защищает только от необъявленных имён. В TDZ переменная объявлена,
  // поэтому исключение всё равно случится — единственный случай, когда typeof бросает.
  typeofInTdz: 'ReferenceError',

  // А здесь имени нет вообще, и typeof возвращает строку 'undefined'.
  typeofUndeclared: 'undefined',

  // Объявление функции поднимается вместе с телом, поэтому вызов до объявления работает.
  functionHoisting: 'вызвалась',

  // После return стоит перенос строки, и ASI вставляет точку с запятой:
  // функция возвращает undefined, а 42 становится недостижимой инструкцией.
  asiAfterReturn: undefined,

  // Обратный случай: перенос перед `(` точку с запятой не вставляет,
  // строки склеиваются в `first + second(list[0])`, и вызов числа падает.
  asiBeforeParenthesis: 'TypeError',

  // В нестрогой функции, вызванной без получателя, this подменяется на глобальный объект.
  thisInSloppyFunction: 'object',

  // В строгом режиме подмены нет: this остаётся undefined.
  thisInStrictFunction: 'undefined',

  // Повторяющееся имя параметра в строгом коде — ранняя ошибка. Она возникает
  // на разборе, до выполнения, поэтому 'use strict' внутри тела успевает подействовать,
  // а сам фрагмент не запускается вообще.
  duplicateParamsInStrict: 'SyntaxError',

  // var-переменная в цикле одна на всю функцию: все три замыкания смотрят в неё
  // и видят значение после выхода из цикла.
  varLoopClosures: [3, 3, 3],

  // let создаёт новую привязку на каждой итерации, поэтому каждое замыкание
  // держит собственную копию.
  letLoopClosures: [0, 1, 2],

  // В нестрогой функции с простым списком параметров arguments связан с параметрами:
  // запись в arguments[0] меняет value.
  argumentsAliasSloppy: 99,

  // В строгом режиме связь разорвана, arguments — просто копия аргументов.
  argumentsAliasStrict: 1,
};
