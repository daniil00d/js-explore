// Заготовка для ответов. Заполни поля, запусти проверку:
//
//   node 00-setup/labs/01-flag-detective/check.mjs
//
// Все ответы про функции из subject.mjs: sumStable, sumMixed, readPoint, neverHot.
// Массивы проверяются как множества, порядок не важен.

export const answers = {
  // 1. Какие из четырёх функций V8 успел скомпилировать оптимизирующим
  //    компилятором TurboFan? Имена строками, например ['fn1', 'fn2'].
  optimizedByTurbofan: [],

  // 2. Какие функции так и остались без оптимизации?
  neverOptimized: [],

  // 3. Какие функции хотя бы раз деоптимизировались, то есть вернулись
  //    из оптимизированного кода в интерпретатор?
  deoptimized: [],

  // 4. Как V8 называет причину деоптимизации readPoint? Строка ровно в том
  //    виде, в каком она стоит в логе после «reason: ».
  readPointReason: '',

  // 5. То же для sumMixed.
  sumMixedReason: '',
};
