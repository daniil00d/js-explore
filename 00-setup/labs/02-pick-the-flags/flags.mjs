// Заготовка для лабы «Подбери флаги». В каждое поле — массив флагов,
// с которыми проверка запустит probe.mjs. Например: ['--trace-gc'].
//
//   node 00-setup/labs/02-pick-the-flags/check.mjs
//
// Полный список флагов и их описания: node --v8-options

export const flags = {
  // 1. Напечатать байткод только функции hotLoop — и ничего больше.
  //    Одного флага здесь не хватит.
  printOneBytecode: [],

  // 2. Заставить hotLoop пройти через оптимизирующий компилятор, хотя её
  //    вызывают всего три раза. Код менять нельзя, natives syntax не нужен.
  optimizeColdFunction: [],

  // 3. Наоборот: не дать горячей spin выйти за пределы интерпретатора.
  keepInInterpreter: [],

  // 4. Увидеть, когда работает сборщик мусора и сколько это занимает.
  showGarbageCollection: [],

  // 5. Заставить молодое поколение переполняться чаще, чтобы сборок мусора
  //    стало заметно больше (проверка ждёт хотя бы в полтора раза).
  moreScavenges: [],
};
